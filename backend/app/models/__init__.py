"""
SQLAlchemy models (grouped by domain).

Packages:
  core          — users, accounts, memberships
  billing       — instructions, cards, top-ups, templates, checkout acceptance
  contracts     — contracts and payment stages
  integrations  — Meta / Google Ads linkage
  campaign      — tracked ad campaigns
  admin         — audit log
  analytics     — cached Meta snapshots
"""
